A very simple app that fetches and displays data from https://swapi.dev/api/ using Axios.
Exemplify the use of React hooks `useState` and `useEffect`.

Use npm start then open in browser http://localhost:3000.
